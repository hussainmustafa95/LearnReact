version: 2.1

executors:
  basic:
    docker:
      - image: cimg/base:current
    resource_class: small

jobs:
  create-env-var:
    executor: basic
    steps:
      - checkout
      - run: 
          name: create env var
          command: |
            ls
            pwd
      - run: 
          name: create env var 2
          command: |
            pwd 
            ls
            cd react-app
            
            ls
      - run: 
          name: create env var 3
          command: |
            pwd 
            ls
            cd react-app
            ls

workflows:
  main:
    jobs:
      - create-env-var
      - load-env-var:
          requires:
            - create-env-var